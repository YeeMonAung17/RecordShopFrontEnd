<div class="records-search">
   
    <div class="records-search-row">
        <input class="records-search-input"
               placeholder="Search by title, artist, genre, year, price, or stock..."
               @bind="searchRecord" />

        <button type="button" class="records-search-button primary" @onclick="OnSearchClicked">
            Search
        </button>

        <button type="button" class="records-search-button secondary" @onclick="OnClearClicked">
            Clear
        </button>
    </div>
</div>

@code {
    [Parameter]
    public EventCallback<string> OnSearch { get; set; }

    private string searchRecord = string.Empty;

    private async Task OnSearchClicked()
    {
        await OnSearch.InvokeAsync(searchRecord);
    }

    private async Task OnClearClicked()
    {
        searchRecord = string.Empty;
        await OnSearch.InvokeAsync(string.Empty);
    }
}